import matplotlib.pyplot as plt

def show_rewards(rewards):
    print("Rewards:", len(rewards), "states evaluated.")

def show_strategy(policy):
    print("Policy:", len(policy), "states evaluated.")
